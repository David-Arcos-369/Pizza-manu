import '../images/logo01.jpg';
import '../assets/css/logo.css';

const logo = (e) => {
            return (
               <div className="logo-container">
               <img src={e.logo01} alt="Icon" />
               </div>
            );

};
export default logo;
